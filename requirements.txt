# AirClaw — NYC 311 Productivity Agent
# Install: pip3 install -r requirements.txt

# Core agent dependencies
apache-airflow>=2.8.0
requests>=2.31.0
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# Data handling (used in tools + data generation)
# csv and collections are stdlib — no install needed

# The standalone runners (run_demo.py, run_model_eval.py) need only
# requests + pydantic + python-dotenv. Airflow is required solely for
# the DAG path (./run_airflow.sh).